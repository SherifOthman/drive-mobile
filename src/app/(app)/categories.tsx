import React from "react";
import { Text, View } from "react-native";

const categories = () => {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-accent">categories</Text>
    </View>
  );
};

export default categories;
